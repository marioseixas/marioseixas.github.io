---
categories:
  - Dotfiles
tags:
  - linux
comment: 
info: aberto.
date: '2024-03-10'
type: post
layout: post
published: true
sha:
slug: bashrc
title: /root/.bashrc
---

```

. ~/.bash_profile
HISTCONTROL=ignoreboth
shopt -s histappend
export XDG_CONFIG_HOME="$HOME/.config"
export GOPATH=$HOME/go
export PATH=$PATH:/usr/local/go/bin:$GOPATH/bin
export PATH="/root/.cargo/bin:$PATH"
PS1='$ '
HISTSIZE=1000
HISTFILESIZE=2000
alias c='reset'
alias p='ping ib.bsb.br'
alias P='systemctl restart networking'
alias þ='poweroff'
alias n='ncspot'
alias Z='swaks -4 -tls -f marioseixascardosoneto@gmail.com -t m.seixas@outlook.com -s smtp.gmail.com:587 -au marioseixascardosoneto@gmail.com -ap pkwuqrfluuoxhtgm -d'
alias q='echo -n "%DATE%\nSubject:" | xsel && xdotool click 2'
alias pull='cd /root/data/marioseixas.github.io && git pull origin main'
alias push='cd /root/data/marioseixas.github.io && git push origin main'
alias blog='sh /userdata/document/SCRIPTS/blog.sh'
alias trans='transfersh --provider storj --storj-access  --storj-bucket	transfer'

transfer(){ if [ $# -eq 0 ];then echo "No arguments specified.\nUsage:\n transfer <file|directory>\n ... | transfer <file_name>">&2;return 1;fi;if tty -s;then file="$1";file_name=$(basename "$file");if [ ! -e "$file" ];then echo "$file: No such file or directory">&2;return 1;fi;if [ -d "$file" ];then file_name="$file_name.zip" ,;(cd "$file"&&zip -r -q - .)|curl --progress-bar --upload-file "-" "http://127.0.0.1:8080/$file_name"|tee /dev/null,;else cat "$file"|curl --progress-bar --upload-file "-" "http://127.0.0.1:8080/$file_name"|tee /dev/null;fi;else file_name=$1;curl --progress-bar --upload-file "-" "http://127.0.0.1:8080/$file_name"|tee /dev/null;fi;}

. "$HOME/.cargo/env"
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
fi

# argc-completions
export ARGC_COMPLETIONS_ROOT="/root/argc-completions"
export ARGC_COMPLETIONS_PATH="$ARGC_COMPLETIONS_ROOT/completions"
export PATH="$ARGC_COMPLETIONS_ROOT/bin:$PATH"
# To add a subset of completions only, change next line e.g. argc_scripts=( cargo git )
argc_scripts=( $(ls -p -1 "$ARGC_COMPLETIONS_ROOT/completions" | sed -n 's/\.sh$//p') )
source <(argc --argc-completions bash "${argc_scripts[@]}")

# Start SSH agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

```
